{
  "name": "deco-workshops",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "clean": "node scripts/cleanup-old-files.js",
    "dev": "npm run clean && vite",
    "build": "npm run clean && tsc -b && vite build && cd server && npm install",
    "start": "cd server && npm start",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.5.3",
    "vite": "^5.4.1"
  }
}
