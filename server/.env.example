# MongoDB Atlas connection string. Replace with your own if needed.
MONGODB_URI=mongodb+srv://aymenbekkai27_db_user:aymenbekkai%40protection%23@cluster0.qgf9w1l.mongodb.net/decoworkshops?retryWrites=true&w=majority&appName=Cluster0

# Port the Express server listens on (Render/Vercel set PORT automatically).
PORT=3001

# Comma-separated list of allowed CORS origins. The Vite dev server runs on 5173.
CLIENT_ORIGIN=http://localhost:5173
